import React from "react";

const About = () => {
  return (
    <div>
      <h1>This is About page</h1>
      <h2> Page loading through LINK component, refreshes only body(Outlet) component</h2>
    </div>
  );
};

export default About;
